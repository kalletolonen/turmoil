export const GameConfig = {
    // Debug Flags
    DEBUG_INFINITE_AP: true,
    RED_FACTION_MAX_AP: true, // New config for the requested feature
    MAX_PROJECTILE_SPEED: 150,

    // Physics
    TURRET_FRICTION: 2.0, // High friction to prevent sliding
    TURRET_DAMPING: 0.5,

    // Map Generation
    SEED: 12345,
};
